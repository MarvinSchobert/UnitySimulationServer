<%- include('partials/header') %>
    <div>
    Hier ist der Bestand im ERP System
    </div>
<div class="row mx-auto">    
    
    <div class="col-lg-4">
        <% if (stammdaten.length == 0) { %>
            Noch keine Materialstammdaten verfügbar.
        <% } else { %>
        <form action="http://localhost:3000/erpSystem/wareVerbuchen" method="POST">           
            <div class="form-group">
            <select name = "itemName" id="itemName" class="form-control ">
                <% for (var i = 0; i < stammdaten.length; i++){ %>
                    <option value = "<%= stammdaten[i] %>" selected ><%= stammdaten[i] %> </option>
                <% } %>
            </select></div>
            <div class="form-group">
                <input type="number" name="itemAnzahl" value="" placeholder=0 id="itemAnzahl" autofocus="autofocus" class="form-control "/>
                
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">Ware hinzufügen</button>
            </div>
        </form>
        <% } %>
    </div>   
    <div class="col-lg-4">
        <% if (stammdaten.length == 0) { %>
            Noch keine Materialstammdaten verfügbar.
        <% } else { %>
            <form action="http://localhost:3000/erpSystem/wareEntnehmen" method="POST">
                <div class="form-group">
                    <input type="text" name="itemName" value="" placeholder="" id="itemName" autofocus="autofocus" class="form-control "/>
                </div>
                <div class="form-group">
                    <input type="number" name="itemAnzahl" value="" placeholder=0 id="itemAnzahl" autofocus="autofocus" class="form-control "/>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-secondary">Ware entnehmen</button>
                </div>
            </form>
        <% } %>
    </div> 
    <div class="col-lg-4">
        <form action="http://localhost:3000/erpSystem/materialstammHinzufuegen" method="POST">
            <div class="form-group">
                <input type="text" name="itemName" value="" placeholder="" id="itemName" autofocus="autofocus" class="form-control "/>
            </div>          
            <div class="form-group">
                <button type="submit" class="btn btn-secondary">Materialart hinzufügen</button>
            </div>
        </form>
    </div> 
</div>
<br>
<div class="col-lg-4">
<table border="1">
    <tr>
        <th>Name</th>
        <th>Anzahl</th>
    </tr>
    <% for (var i = 0; i < inventory.length; i++) { %>
        <tr>
            <th> <%= inventory[i].name %> </th>
            <th> <%= inventory[i].anzahl %> </th>
        </tr>
    <% } %>
</table>
</div>